import { css } from "@emotion/react";

const styles = {
  main:{
    container: css({
      minHeight: '100vh',
    }),
    content: css({
      maxWidth: 1080,
      margin: 'auto',
      padding: '6rem 2.5rem 5rem'
    })
  } 
}

export default styles